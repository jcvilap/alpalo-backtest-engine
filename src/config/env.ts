/**
 * Environment Configuration
 *
 * Centralized configuration for environment variables.
 */

// Currently empty as TRADING_MODE was the only export.
// Future environment configuration can be added here.
export { };

